export interface LoginFormValues {
  email: string;
  password: string;
  rememberMe: boolean;
}

export interface ProductBadge {
  emoji: string;
  name: string;
  price: string;
  className: string;
  animationDelay: string;
}
